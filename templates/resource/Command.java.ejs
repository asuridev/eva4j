package <%= packageName %>.<%= moduleName %>.application.commands;

import <%= packageName %>.shared.interfaces.Command;

public record <%= usecaseName %>Command(<% if (hasId) { %>
    String id<% } %>
    // TODO: Add additional command fields here
) implements Command {
}
