package <%= packageName %>.<%= moduleName %>.application.usecases;

import <%= packageName %>.<%= moduleName %>.application.dtos.<%= resourceName %>ResponseDto;
import <%= packageName %>.<%= moduleName %>.application.queries.<%= usecaseName %>Query;
import <%= packageName %>.shared.domain.annotations.ApplicationComponent;
import <%= packageName %>.shared.domain.interfaces.QueryHandler;
import lombok.extern.slf4j.Slf4j;
<% if (isFindAll) { %>
import java.util.List;
<% } %>

@ApplicationComponent
@Slf4j
public class <%= usecaseName %>QueryHandler implements QueryHandler<<%= usecaseName %>Query, <% if (isFindAll) { %>List<<%= resourceName %>ResponseDto><% } else { %><%= resourceName %>ResponseDto<% } %>> {

    // TODO: Inject dependencies (repositories, services, etc.)

    public <%= usecaseName %>QueryHandler() {
    }

    @Override
    public <% if (isFindAll) { %>List<<%= resourceName %>ResponseDto><% } else { %><%= resourceName %>ResponseDto<% } %> handle(<%= usecaseName %>Query query) {
        log.info("Handling <%= usecaseName %>Query: {}", query);
        // TODO: Implement query logic
        <% if (isFindAll) { %> 
         return List.of(new <%= resourceName %>ResponseDto()); // Placeholder return
        <% } else { %>
         return new <%= resourceName %>ResponseDto(); // Placeholder return
        <% } %>
    }
}
