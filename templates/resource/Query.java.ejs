package <%= packageName %>.<%= moduleName %>.application.queries;
<% if (isFindAll) { %>
import java.util.List;
<% } %>
import <%= packageName %>.<%= moduleName %>.application.dtos.<%= resourceName %>ResponseDto;
import <%= packageName %>.shared.domain.interfaces.Query;

public record <%= usecaseName %>Query(<% if (hasId) { %>
    String id<% } %>
    // TODO: Add additional query parameters here
) implements Query<<% if (isFindAll) { %>List<<%= resourceName %>ResponseDto><% } else { %><%= resourceName %>ResponseDto<% } %>> {
}
